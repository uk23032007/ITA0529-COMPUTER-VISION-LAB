import cv2
img = cv2.imread('sample.jpg')
cv2.imshow('Original', img)
cv2.imshow('Smaller', cv2.resize(img, (0,0), fx=0.5, fy=0.5))
cv2.imshow('Bigger', cv2.resize(img, (0,0), fx=2.0, fy=2.0))
cv2.waitKey(0)
cv2.destroyAllWindows()
